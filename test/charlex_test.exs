defmodule CharlexTest do
  use ExUnit.Case
  doctest Charlex

  test "greets the world" do
    assert "hello" == "hello"
  end

  test "test for running ci" do
    assert "world" == "world"
  end
end
